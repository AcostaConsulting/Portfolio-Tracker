// Versión de la app: fuente única para la UI (Acerca, Reportar problema, Ayuda).
// `package.json` es la fuente para el build/instalador; mantenlos en sync (Bloque H).
export const APP_VERSION = '0.9.0';
